
name: package2-build

build:
  docker:
    - image: circleci/node:latest
  steps:
    - checkout
    - run:
        name: Build package_2
        command: cd package_2 && just build


workflow:
  context:
    - context-1
    - san-slack